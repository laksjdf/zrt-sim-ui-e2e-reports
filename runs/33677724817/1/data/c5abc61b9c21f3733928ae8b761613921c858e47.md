# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: l1/training-hardware-search.spec.ts >> @L1 训练硬件寻优
- Location: e2e/specs/l1/training-hardware-search.spec.ts:13:1

# Error details

```
Error: expect(locator).toContainText(expected) failed

Locator: getByTestId('hardware-candidate-select').first()
Expected substring: "B200"
Received string:    "NVIDIA B300 POD (B300, 72 GPU NVL)"
Timeout: 5000ms

Call log:
  - Expect "toContainText" with timeout 5000ms
  - waiting for getByTestId('hardware-candidate-select').first()
    14 × locator resolved to <div data-v-6a9bea98="" data-v-be1ef079="" data-testid="hardware-candidate-select" class="el-select hardware-metric-select">…</div>
       - unexpected value "NVIDIA B300 POD (B300, 72 GPU NVL)"

```

```yaml
- combobox
- text: NVIDIA B300 POD (B300, 72 GPU NVL)
- img
```

# Test source

```ts
  1  | import { test, expect } from '../../fixtures/test'
  2  | import { waitForPageJobStatus } from '../../support/job-poller'
  3  | import { E2E_TEST_TIMEOUT_MS } from '../../support/timeouts'
  4  | import { selectOption } from '../../support/select-option'
  5  | 
  6  | async function selectFormValue(page, label: string, value: string) {
  7  |   const field = page.locator('label.form-group').filter({
  8  |     has: page.getByText(label, { exact: true }),
  9  |   })
  10 |   await field.locator('select').selectOption(value)
  11 | }
  12 | 
  13 | test('@L1 训练硬件寻优', async ({ page, api, catalogs }) => {
  14 |   test.setTimeout(E2E_TEST_TIMEOUT_MS)
  15 | 
  16 |   await page.goto('/zrt-sim/train/optimize/hardware')
  17 |   await expect(page.getByRole('heading', { name: /硬件寻优|Hardware/ })).toBeVisible()
  18 | 
  19 |   // 使用训练模型目录返回的规范 ID，避免与模型配置文件名（下划线）混用。
  20 |   await page.getByTestId('hardware-model-select').selectOption('llama3-70b')
  21 |   await page.getByTestId('hardware-top-n-input').fill('2')
  22 |   // 默认无重计算时 1F1B 长序列激活 ≈228GB/卡，80GB 卡必然 OOM；full 重计算后 ≈33GB，两个 POD 候选均可容纳
  23 |   await selectFormValue(page, '重计算', 'full')
  24 |   // 两个 POD 候选均为 72 卡；TP=2、PP=2 时 DP=18，满足训练策略的 DP 约束。
  25 |   await selectFormValue(page, 'TP', '2')
  26 |   await selectFormValue(page, 'PP', '2')
  27 |   await selectFormValue(page, 'EP', '1')
  28 |   await selectFormValue(page, 'ZeRO', '1')
  29 | 
  30 |   // 页面初始携带 4 个默认候选；精简到 1 个，再换成两个有可行 DP 的 POD 候选。
  31 |   // 全参训练下两个 POD 候选的容量足以承载 full 重计算后的工作集，8 卡 Server 候选不纳入本场景。
  32 |   const removeButtons = page.locator('.row-grid.hardware button.row-act')
  33 |   while (await removeButtons.count() > 1) {
  34 |     await removeButtons.first().click()
  35 |   }
  36 |   await page.getByTestId('hardware-add-candidate').click()
  37 |   const selects = page.getByTestId('hardware-candidate-select')
  38 |   await expect(selects).toHaveCount(2)
  39 |   await selectOption(selects.first(), 'B200_POD')
> 40 |   await expect(selects.first()).toContainText('B200')
     |                                 ^ Error: expect(locator).toContainText(expected) failed
  41 |   await selectOption(selects.nth(1), 'B300_POD')
  42 |   await expect(selects.nth(1)).toContainText('B300')
  43 |   // 72 卡下 DP=18，默认全局批次 2048 无法被 DP×micro_batch 整除；填 1800 使策略校验通过。
  44 |   await page.locator('label.form-group').filter({
  45 |     has: page.getByText('全局批次', { exact: true }),
  46 |   }).locator('input').fill('1800')
  47 | 
  48 |   const submitted = page.waitForResponse(response =>
  49 |     response.request().method() === 'POST'
  50 |     && response.url().includes('/api/train/search/hardware')
  51 |     && response.status() === 202,
  52 |   )
  53 |   await page.getByTestId('hardware-run-button').click()
  54 |   const job = await (await submitted).json()
  55 | 
  56 |   await waitForPageJobStatus(page, 'hardware-job-status', 'Success')
  57 | 
  58 |   const backendJob = await api.waitForJob(job.run_id, 'succeeded')
  59 |   const frontier = backendJob.result?.pareto_frontier || []
  60 |   expect(backendJob.result?.total_configs).toBe(2)
  61 |   expect(backendJob.result?.pareto_count).toBe(2)
  62 |   expect(frontier).toHaveLength(2)
  63 | 
  64 |   await api.deleteTask(job.task_id)
  65 | })
  66 | 
```