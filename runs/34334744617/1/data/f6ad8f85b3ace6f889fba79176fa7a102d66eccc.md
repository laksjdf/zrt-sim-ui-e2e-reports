# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: l0/user-manual-navigation.spec.ts >> @L0 普通用户可打开用户手册
- Location: e2e/specs/l0/user-manual-navigation.spec.ts:3:1

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByRole('link', { name: '用户手册' })
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 5000ms
  - waiting for getByRole('link', { name: '用户手册' })

```

# Test source

```ts
  1  | import { test, expect } from '../../fixtures/test'
  2  | 
  3  | test('@L0 普通用户可打开用户手册', async ({ page }) => {
  4  |   await page.goto('/zrt-sim/?account=guide-user&name=Guide%20User')
  5  | 
> 6  |   await expect(page.getByRole('link', { name: '用户手册' })).toBeVisible()
     |                                                          ^ Error: expect(locator).toBeVisible() failed
  7  | 
  8  |   await page.goto('/zrt-sim/user-manual?account=guide-user&name=Guide%20User')
  9  |   await expect(page.getByRole('heading', { name: 'AI 负载建模仿真平台用户手册' })).toBeVisible()
  10 |   await expect(page.locator('.manual-content img')).toHaveCount(18)
  11 |   await expect.poll(async () => page.locator('.manual-content img').evaluateAll(images =>
  12 |     images.filter(image => (image as HTMLImageElement).naturalWidth > 0).length,
  13 |   )).toBe(18)
  14 |   await expect(page.locator('.manual-content img').first()).toHaveAttribute('src', /user-manual\/images/)
  15 | 
  16 |   const firstTocLink = page.locator('.manual-content a').filter({ hasText: '进入平台、准备账号和认识常用控件' }).first()
  17 |   await firstTocLink.click()
  18 |   await expect.poll(() => page.evaluate(() => decodeURIComponent(window.location.hash))).toBe('#1-先了解平台')
  19 |   await expect(page.locator('[id="1-先了解平台"]')).toBeVisible()
  20 | })
  21 | 
```