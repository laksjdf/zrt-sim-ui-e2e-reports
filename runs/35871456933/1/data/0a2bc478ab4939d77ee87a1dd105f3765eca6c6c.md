# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: l1/training-analysis-progress.spec.ts >> @L1 真实训练分析 baseline 两帧进度与持久化结果
- Location: e2e/specs/l1/training-analysis-progress.spec.ts:80:3

# Error details

```
Error: expect(received).toBe(expected) // Object.is equality

Expected: true
Received: false
```

# Test source

```ts
  1   | import { test, expect } from '../../fixtures/test'
  2   | import type { Page } from '@playwright/test'
  3   | import type { TestFixtures } from '../../fixtures/test'
  4   | 
  5   | const backend = process.env.E2E_BACKEND_URL || 'http://127.0.0.1:18001'
  6   | const strategy = { tp: 2, cp: 1, pp: 1, ep: 1, dp: 4, micro_batch: 1, global_batch: 32 }
  7   | 
  8   | function syntheticModel(asset: any) {
  9   |   // 请求内合成 640 层工作量，使两次真实轮询有机会观察进度，不修改数据库资产或算子模板。
  10  |   // runtime_model_document 优先使用 HF；zrt_train.layers 又优先于 num_hidden_layers，须同步层数。
  11  |   return { ...asset.model_spec_json, hf_config_json: {
  12  |     ...asset.hf_config_json, num_hidden_layers: 640,
  13  |     zrt_train: { ...asset.hf_config_json.zrt_train, layers: '[dense]*640' },
  14  |   } }
  15  | }
  16  | 
  17  | async function observeFrames(page: Page) {
  18  |   // 仅观察真实 DOM 更新，不修改轮询频率、响应或计算耗时；保留完成前短暂出现的帧。
  19  |   await page.addInitScript(() => {
  20  |     const frames: any[] = []
  21  |     ;(window as any).__analysisFrames = frames
  22  |     new MutationObserver(() => {
  23  |       const state = document.querySelector('.exec-state')
  24  |       const text = state?.querySelector('.exec-sub')?.textContent || ''
  25  |       const match = text.match(/已完成 (\d+) \/ (\d+)（(\d+)%）/)
  26  |       if (!match) return
  27  |       const frame = { completed: Number(match[1]), total: Number(match[2]), percent: Number(match[3]),
  28  |         label: state?.querySelector('.exec-label')?.textContent,
  29  |         width: (state?.querySelector('.exec-bar-fill') as HTMLElement)?.style.width }
  30  |       if (JSON.stringify(frames.at(-1)) !== JSON.stringify(frame)) frames.push(frame)
  31  |     }).observe(document, { subtree: true, childList: true, characterData: true, attributes: true })
  32  |   })
  33  | }
  34  | 
  35  | async function submitAnalysis(
  36  |   page: Page, api: TestFixtures['api'], testOwner: TestFixtures['testOwner'], kind: 'baseline' | 'compare',
  37  | ) {
  38  |     const headers = { 'X-User-Account': testOwner.account }
  39  |     const hardwareResponse = await page.request.get(`${backend}/api/assets/hardwares?domain=train`, { headers })
> 40  |     expect(hardwareResponse.ok()).toBe(true)
      |                                   ^ Error: expect(received).toBe(expected) // Object.is equality
  41  |     const hardwareRows = await hardwareResponse.json()
  42  |     const hardware = hardwareRows.find((row: any) => row.name === 'H100_Server')
  43  |     expect(hardware).toBeTruthy()
  44  |     const comparisonHardware = ['H100_Server', 'H200_Server'].map(name => {
  45  |       const row = hardwareRows.find((item: any) => item.name === name)
  46  |       expect(row, name).toBeTruthy()
  47  |       return row
  48  |     })
  49  |     const models = await api.getTrainModels()
  50  |     const model = models.find(row => row.name === 'llama3-70b')
  51  |     expect(model).toBeTruthy()
  52  |     const modelResponse = await page.request.get(`${backend}/api/assets/models/${model!.id}`, { headers })
  53  |     expect(modelResponse.ok()).toBe(true)
  54  |     const asset = await modelResponse.json()
  55  |     const modelJson = syntheticModel(asset)
  56  |     // 引用 API 中两条真实硬件资产，后端负责持久化快照，不改写规格或 registry reference。
  57  |     const body = kind === 'baseline' ? {
  58  |       hw: { asset_id: hardware.id }, model: model!.name, model_json: modelJson,
  59  |       world_size: 8, nodes: 1, gpus_per_node: 8, seq_len: 4096,
  60  |       // SLA 模式展开真实的梯度累积候选，确保浏览器能观察多个后端进度阶段。
  61  |       micro_batch: 1, with_anchors: true, sla_step_ms: 1_000_000,
  62  |     } : {
  63  |       models: [model!.name], model_jsons: { [model!.name!]: modelJson }, world_size: 8,
  64  |       hardwares: comparisonHardware.map(row => ({
  65  |         hw: { asset_id: row.id }, label: row.name,
  66  |         nodes: 1, gpus_per_node: 8, world_size: 8, strategy,
  67  |       })),
  68  |     }
  69  |     await observeFrames(page)
  70  |     await page.goto(`/zrt-sim/train/${kind === 'baseline' ? 'optimize/spec' : 'compare'}`)
  71  |     await expect(page.locator('h1')).toBeVisible()
  72  |     const response = await page.request.post(`${backend}/api/train/${kind === 'baseline' ? 'optimize/spec/baseline' : 'compare'}`, {
  73  |       headers, data: { ...body, username: testOwner.username, account: testOwner.account },
  74  |     })
  75  |     expect(response.status(), await response.text()).toBe(202)
  76  |     return response.json()
  77  | }
  78  | 
  79  | for (const kind of ['baseline', 'compare'] as const) {
  80  |   test(`@L1 真实训练分析 ${kind} 两帧进度与持久化结果`, async ({ page, api, testOwner }, testInfo) => {
  81  |     test.setTimeout(180_000)
  82  |     const job = await submitAnalysis(page, api, testOwner, kind)
  83  |     try {
  84  |       await page.goto(`/zrt-sim/train/${kind === 'baseline' ? 'optimize/spec' : 'compare'}?task_id=${job.task_id}&run_id=${job.run_id}`)
  85  |       const finished = await api.waitForJob(job.run_id, 'succeeded', 150_000)
  86  |       await expect(page.locator('.exec-state')).toHaveCount(0, { timeout: 15_000 })
  87  |       if (kind === 'baseline') {
  88  |         expect(finished.result.anchors.length).toBeGreaterThan(0)
  89  |         expect(finished.result.profile.tokens_per_sec).toBeGreaterThan(0)
  90  |         // 锚点可能因真实显存约束无解；结果区仍必须明确渲染吞吐/可行性，而非假定恒为 100%。
  91  |         const metrics = page.locator('.metric-grid')
  92  |         await expect(metrics).toBeVisible()
  93  |         await expect(metrics).toContainText('吞吐（vs 基线最优）')
  94  |         expect(await metrics.textContent()).toMatch(/(—|\d+\.\d+%)/)
  95  |       } else {
  96  |         expect(finished.result.conclusion.rows.length).toBeGreaterThan(0)
  97  |         await expect(page.getByTestId('compare-result-table')).toBeVisible()
  98  |       }
  99  |       const persisted = await api.getJob(job.run_id)
  100 |       expect(persisted.result).toEqual(finished.result)
  101 |       const frames = await page.evaluate(() => (window as any).__analysisFrames)
  102 |       const progress = (persisted as any).progress
  103 |       await testInfo.attach(`${kind}-real-frames`, {
  104 |         body: JSON.stringify({ taskId: job.task_id, runId: job.run_id, status: persisted.status, progress, frames }),
  105 |         contentType: 'application/json',
  106 |       })
  107 |       expect(progress.total).toBeGreaterThan(1)
  108 |       expect([progress.completed, progress.percent]).toEqual([progress.total, 100])
  109 |       expect(new Set(frames.map((frame: any) => frame.completed)).size, JSON.stringify(frames)).toBeGreaterThanOrEqual(2)
  110 |       const total = frames[0].total
  111 |       expect(total).toBeGreaterThan(1)
  112 |       for (const frame of frames) {
  113 |         expect(frame.total).toBe(total)
  114 |         expect(frame.width).toBe(`${frame.percent}%`)
  115 |         expect(frame.label).toBe(kind === 'baseline' ? '正在评估基线与策略锚点' : '正在完成对比分析单元')
  116 |       }
  117 |       expect([progress.completed, progress.total, progress.percent]).toEqual([total, total, 100])
  118 |     } finally {
  119 |       await api.deleteTask(job.task_id)
  120 |     }
  121 |   })
  122 | }
  123 | 
```