# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: l1/training-compare.spec.ts >> @L1 训练多硬件性能对比
- Location: e2e/specs/l1/training-compare.spec.ts:3:1

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByRole('heading', { name: /硬件对比|Compare/ })
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 5000ms
  - waiting for getByRole('heading', { name: /硬件对比|Compare/ })

```

# Test source

```ts
  1  | import { test, expect } from '../../fixtures/test'
  2  | 
  3  | test('@L1 训练多硬件性能对比', async ({ page, api, catalogs }) => {
  4  |   test.setTimeout(180_000)
  5  | 
  6  |   await page.goto('/zrt-sim/train/compare')
> 7  |   await expect(page.getByRole('heading', { name: /硬件对比|Compare/ })).toBeVisible()
     |                                                                     ^ Error: expect(locator).toBeVisible() failed
  8  | 
  9  |   await page.getByTestId('compare-model-select').selectOption(catalogs.train.modelId)
  10 |   await page.getByTestId('compare-world-size-input').fill('128')
  11 | 
  12 |   // 确保有两种硬件（add 后默认会选中下一个未使用的硬件）
  13 |   await page.getByTestId('compare-add-hardware').click()
  14 |   const selects = page.getByTestId('compare-hardware-select')
  15 |   await expect(selects).toHaveCount(2)
  16 |   await selects.first().selectOption(catalogs.train.hardwareId)
  17 | 
  18 |   const submitted = page.waitForResponse(response =>
  19 |     response.request().method() === 'POST'
  20 |     && response.url().includes('/api/train/compare')
  21 |     && response.status() === 202,
  22 |   )
  23 |   await page.getByTestId('compare-run-button').click()
  24 |   const job = await (await submitted).json()
  25 | 
  26 |   await page.getByTestId('compare-result-card').waitFor({ state: 'visible', timeout: 120_000 })
  27 |   await page.getByTestId('compare-result-table').waitFor({ state: 'visible', timeout: 120_000 })
  28 | 
  29 |   const backendJob = await api.waitForJob(job.run_id, 'succeeded', 120_000)
  30 |   expect(backendJob.result?.conclusion?.rows?.length).toBeGreaterThanOrEqual(1)
  31 | 
  32 |   await api.deleteTask(job.task_id)
  33 | })
  34 | 
```