# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: l1/inference-operator.spec.ts >> @L1 推理算子性能评估
- Location: e2e/specs/l1/inference-operator.spec.ts:3:1

# Error details

```
Test timeout of 120000ms exceeded.
```

```
Error: expect(locator).toHaveAttribute(expected) failed

Locator:  getByTestId('op-job-status')
Expected: "succeeded"
Received: "pending"

Call log:
  - Expect "toHaveAttribute" with timeout 120000ms
  - waiting for getByTestId('op-job-status')
    240 × locator resolved to <div data-v-8ff6ecfc="" class="result-card" data-status="pending" data-testid="op-job-status">…</div>
        - unexpected value "pending"

```

```yaml
- text: 算子性能评估 排队等待... 118s 后端运算中，请稍候
```

# Test source

```ts
  1  | import { test, expect } from '../../fixtures/test'
  2  | 
  3  | test('@L1 推理算子性能评估', async ({ page, api }) => {
  4  |   test.setTimeout(120_000)
  5  | 
  6  |   await page.goto('/zrt-sim/infer/performance/op')
  7  |   await expect(page.getByRole('heading', { name: /算子性能评估|Operator/ })).toBeVisible()
  8  | 
  9  |   // 推理算子页使用自定义下拉，非 <select>
  10 |   await page.locator('.inference-operator-trigger').click()
  11 |   await page.locator('.inference-operator-option').filter({ hasText: 'matmul' }).first().click()
  12 |   // 使用目录中第一个真实硬件（不再是 h100）
  13 |   await page.getByTestId('op-hardware-select').selectOption({ index: 1 })
  14 | 
  15 |   const rows = page.getByTestId('op-tensor-row')
  16 |   await rows.first().getByTestId('op-tensor-shape').fill('[1024,1024]')
  17 | 
  18 |   const submitted = page.waitForResponse(response =>
  19 |     response.request().method() === 'POST'
  20 |     && response.url().includes('/api/infer/simulate/op')
  21 |     && response.status() === 202,
  22 |   )
  23 |   await page.getByTestId('op-run-button').click()
  24 |   const job = await (await submitted).json()
  25 | 
  26 |   await page.getByTestId('op-job-status').waitFor({ state: 'visible' })
> 27 |   await expect(page.getByTestId('op-job-status')).toHaveAttribute('data-status', 'succeeded', { timeout: 120_000 })
     |                                                   ^ Error: expect(locator).toHaveAttribute(expected) failed
  28 | 
  29 |   await api.deleteTask(job.task_id)
  30 | })
  31 | 
```