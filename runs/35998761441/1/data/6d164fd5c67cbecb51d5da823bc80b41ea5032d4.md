# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: l1/user-calibration.spec.ts >> @L1 用户手工校准工作台闭环
- Location: e2e/specs/l1/user-calibration.spec.ts:526:1

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByRole('heading', { name: '用户精度校准' })
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 5000ms
  - waiting for getByRole('heading', { name: '用户精度校准' })

```

```yaml
- banner:
  - text: ZRT AI负载建模仿真平台
  - link "返回主页":
    - /url: /zrt-sim/
    - img
    - text: 返回主页
  - link "任务管理":
    - /url: /zrt-sim/tasks
  - link "资产管理":
    - /url: /zrt-sim/assets
  - link "信息统计":
    - /url: /zrt-sim/statistics
  - link "版本信息":
    - /url: /zrt-sim/release-notes
  - link "用户管理":
    - /url: /zrt-sim/user-manage
  - link "用户手册":
    - /url: /zrt-sim/user-manual
  - button "pw_user_15_1790254554822":
    - img
    - text: pw_user_15_1790254554822
  - text: Idle
- main:
  - text: ZRT ✦ Scenario Hub
  - heading "AI负载建模仿真平台" [level=1]
  - paragraph: 选择建模场景，进入对应领域的性能评估、硬件对比与系统寻优工具链。
  - article:
    - text: 🏋️ TRAIN 训练
    - paragraph: 大规模分布式训练性能建模，对比不同并行策略与硬件配置下的训练吞吐与 MFU。
    - text: 性能评估
    - button "算子性能评估 →"
    - button "模型性能评估 →"
    - text: 多硬件性能对比
    - button "多硬件性能对比 →"
    - text: 系统寻优
    - button "模型策略自动寻优 →"
    - button "批量训练实验 →"
    - button "硬件寻优 →"
    - button "硬件配比量化分析 →"
  - article:
    - text: ⚡ INFER 推理
    - paragraph: LLM 推理负载建模，分析 Prefill / Decode 阶段的时延、吞吐与显存占用。
    - text: 性能评估
    - button "算子性能评估 →"
    - button "模型性能评估 →"
    - text: 多硬件性能对比
    - button "多硬件性能对比 →"
    - text: 系统寻优
    - button "模型策略自动寻优 →"
    - button "批量推理实验 →"
    - button "PD分离实例配比寻优 →"
  - article:
    - text: 🎯 REC 推荐
    - paragraph: 推荐系统建模，评估 Embedding、特征交互等算子的计算与通信瓶颈。
    - text: 性能评估
    - button "算子性能评估 →"
    - button "模型性能评估 →"
```

# Test source

```ts
  431 |   await expect(page.getByTestId('calibration-build-readiness-report')).toContainText('当前资产声明')
  432 |   await expect(page.getByTestId('calibration-build-readiness-report')).toContainText('未填写指纹')
  433 |   await expect(page.getByTestId('calibration-build-readiness-report')).toContainText('导入时已绑定选中资产')
  434 |   await page.getByTestId('calibration-shape-fit-preview').click()
  435 |   await expect(page.getByTestId('calibration-shape-fit-report')).toContainText('独立 Shape 少于 5 个')
  436 |   await expect(page.getByTestId('calibration-shape-fit-report')).toContainText('不能发布')
  437 |   await expect(page.getByTestId('calibration-shape-fit-report')).toContainText('不能证明真机采集来源')
  438 |   await page.getByTestId('calibration-source-task-id').fill('9999999')
  439 |   await expect(page.getByTestId('calibration-shape-fit-report')).toHaveCount(0)
  440 |   await page.getByTestId('calibration-shape-fit-preview').click()
  441 |   await expect(page.getByRole('alert').last()).toContainText('源训练任务不存在或无权访问')
  442 |   await page.getByTestId('calibration-source-task-id').fill('')
  443 |   await page.getByTestId('calibration-profile-file').setInputFiles({
  444 |     name: 'MatMulV3.csv', mimeType: 'text/csv',
  445 |     buffer: Buffer.from(csv.toString().replace('12.5', '13.5')),
  446 |   })
  447 |   await page.getByTestId('calibration-version-save').click()
  448 |   await row.getByRole('button', { name: '编辑' }).click()
  449 |   await expect(page.getByText('有效 1 / 总 1')).toHaveCount(2)
  450 |   await expect(page.getByTestId('calibration-build-readiness-report')).toHaveCount(0)
  451 |   await expect(page.getByTestId('calibration-shape-fit-report')).toHaveCount(0)
  452 |   const downloadEvent = page.waitForEvent('download')
  453 |   await page.getByRole('button', { name: '下载原始文件' }).first().click()
  454 |   const download = await downloadEvent
  455 |   expect(download.suggestedFilename()).toBe('MatMulV3.csv')
  456 |   await page.getByRole('button', { name: '从草稿移除' }).first().click()
  457 |   await expect(page.getByText('有效 1 / 总 1')).toHaveCount(1)
  458 |   await page.getByRole('button', { name: '从草稿移除' }).click()
  459 |   await expect(page.getByText('有效 1 / 总 1')).toHaveCount(0)
  460 |   await page.getByRole('button', { name: '取消' }).click()
  461 |   await row.getByRole('button', { name: '删除' }).click()
  462 |   await acceptWorkbenchConfirmation(page)
  463 |   await expect(row).toHaveCount(0)
  464 | })
  465 | 
  466 | test('@L1 旧草稿拟合响应不能污染新草稿报告', async ({ page, catalogs, testOwner }) => {
  467 |   // 观测点：两个相同 revision 的私有草稿切换时，A 的迟到响应不能落到 B 的弹窗。
  468 |   const backend = process.env.E2E_BACKEND_URL
  469 |   if (!backend) throw new Error('E2E_BACKEND_URL is required')
  470 |   const stamp = Date.now()
  471 |   const csv = Buffer.from(
  472 |     'op_type,op_name,op_state,tasktype,torch_input_params,input_shapes,input_data_types,input_formats,output_shapes,output_data_types,output_formats,task_duration_us,trimmed_sample_count\n'
  473 |     + 'MatMulV3,aclnnMatmul,dynamic,AI_CORE,"{""transpose_x1"": false, ""transpose_x2"": false}","8,16;16,4",DT_BF16;DT_BF16,ND;ND,"8,4",DT_BF16,ND,"{""mean"": 12.5}",21\n',
  474 |   )
  475 |   const headers = buildTestUserHeaders(testOwner)
  476 |   async function createDraft(name: string): Promise<string> {
  477 |     const created = await page.request.post(`${backend}/api/calibrations/versions`, {
  478 |       headers,
  479 |       data: {
  480 |         name, source_kind: 'profiling', hardware_id: catalogs.train.hardwareName,
  481 |         model_id: catalogs.train.modelId, phase: 'train',
  482 |         scope: { hardware: catalogs.train.hardwareName, model: catalogs.train.modelId, phase: 'train' },
  483 |       },
  484 |     })
  485 |     expect(created.ok()).toBeTruthy()
  486 |     const id = (await created.json()).id as string
  487 |     const imported = await page.request.post(
  488 |       `${backend}/api/calibrations/versions/${id}/batches?filename=MatMulV3.csv&request_key=capture&revision=1`,
  489 |       { headers, data: csv },
  490 |     )
  491 |     expect(imported.ok()).toBeTruthy()
  492 |     return id
  493 |   }
  494 |   const nameA = `拟合-A-${stamp}`
  495 |   const nameB = `拟合-B-${stamp}`
  496 |   const idA = await createDraft(nameA)
  497 |   await createDraft(nameB)
  498 |   let releaseOld: (() => void) | undefined
  499 |   let signalIntercept: (() => void) | undefined
  500 |   const intercepted = new Promise<void>(resolve => { signalIntercept = resolve })
  501 |   await page.route('**/shape-fit-preview', async route => {
  502 |     if (!route.request().url().includes(idA)) return route.continue()
  503 |     const response = await route.fetch()
  504 |     await new Promise<void>(resolve => {
  505 |       releaseOld = resolve
  506 |       signalIntercept?.()
  507 |     })
  508 |     await route.fulfill({ response })
  509 |   })
  510 |   await page.goto('/zrt-sim/calibration')
  511 |   await page.getByTestId('calibration-nav-versions').click()
  512 |   await page.locator('tbody tr').filter({ hasText: nameA }).getByRole('button', { name: '编辑' }).click()
  513 |   await expect(page.getByText('有效 1 / 总 1')).toBeVisible()
  514 |   await page.getByTestId('calibration-shape-fit-preview').click()
  515 |   await intercepted
  516 |   await page.getByRole('button', { name: '取消' }).click()
  517 |   await page.locator('tbody tr').filter({ hasText: nameB }).getByRole('button', { name: '编辑' }).click()
  518 |   await expect(page.getByText('有效 1 / 总 1')).toBeVisible()
  519 |   const oldResponse = page.waitForResponse(response => response.url().includes(idA)
  520 |     && response.url().includes('shape-fit-preview'))
  521 |   releaseOld?.()
  522 |   await oldResponse
  523 |   await expect(page.getByTestId('calibration-shape-fit-report')).toHaveCount(0)
  524 | })
  525 | 
  526 | test('@L1 用户手工校准工作台闭环', async ({ page, catalogs }) => {
  527 |   test.setTimeout(E2E_MULTI_TASK_TIMEOUT_MS)
  528 |   const versionName = `E2E手工校准-${Date.now()}`
  529 | 
  530 |   await page.goto('/zrt-sim/calibration')
> 531 |   await expect(page.getByRole('heading', { name: '用户精度校准' })).toBeVisible()
      |                                                               ^ Error: expect(locator).toBeVisible() failed
  532 | 
  533 |   await page.getByTestId('calibration-create-open').click()
  534 |   await page.getByTestId('calibration-version-name').fill(versionName)
  535 |   await page.getByTestId('calibration-version-hardware').fill(catalogs.train.hardwareName)
  536 |   await page.getByTestId('calibration-version-model').fill(catalogs.train.modelId)
  537 |   await page.getByTestId('calibration-version-phase').fill('train')
  538 |   await page.getByTestId('calibration-entry-operator').fill('aten.mm.default')
  539 |   await page.getByTestId('calibration-entry-unit').selectOption('Cube')
  540 |   await page.getByTestId('calibration-entry-dtype').fill('*')
  541 |   await page.getByTestId('calibration-entry-utilization').fill('25')
  542 |   await page.getByTestId('calibration-entry-add').click()
  543 |   await page.getByTestId('calibration-version-save').click()
  544 |   await page.getByTestId('calibration-nav-versions').click()
  545 | 
  546 |   const versionRow = page.locator('tbody tr').filter({ hasText: versionName })
  547 |   await expect(versionRow).toBeVisible()
  548 |   await versionRow.getByRole('button', { name: '编辑' }).click()
  549 |   await page.getByTestId('calibration-saved-entry-utilization').fill('30')
  550 |   await page.getByTestId('calibration-version-save').click()
  551 |   await versionRow.getByRole('button', { name: '发布' }).click()
  552 |   await expect(versionRow).toContainText('已发布')
  553 |   await versionRow.getByRole('button', { name: '启用' }).click()
  554 |   await expect(versionRow).toContainText('已启用')
  555 | 
  556 |   await page.getByTestId('calibration-nav-rules').click()
  557 |   await page.getByTestId('calibration-rule-hardware').fill(catalogs.train.hardwareName)
  558 |   await page.getByTestId('calibration-rule-model').fill(catalogs.train.modelId)
  559 |   await page.getByTestId('calibration-rule-phase').fill('train')
  560 |   await expect(page.getByTestId('calibration-rule-save')).toBeEnabled()
  561 |   await page.getByTestId('calibration-rule-version').filter({ hasText: versionName }).click()
  562 |   await page.getByTestId('calibration-rule-save').click()
  563 |   await page.getByTestId('calibration-preview-operator').fill('aten.mm.default')
  564 |   await page.getByTestId('calibration-preview-dtype').fill('bf16')
  565 |   await page.getByTestId('calibration-preview-run').click()
  566 |   await expect(page.getByTestId('calibration-preview-result')).toContainText('30%')
  567 | 
  568 |   await page.goto('/zrt-sim/calibration')
  569 |   await page.getByTestId('calibration-nav-versions').click()
  570 |   const frozenRow = page.locator('tbody tr').filter({ hasText: versionName })
  571 |   await frozenRow.getByRole('button', { name: '停用' }).click()
  572 |   await expect(frozenRow).toContainText('用于后续任务：未启用')
  573 |   await frozenRow.getByRole('button', { name: '归档' }).click()
  574 |   await acceptWorkbenchConfirmation(page)
  575 |   await expect(frozenRow).toContainText('已归档')
  576 |   await frozenRow.getByRole('button', { name: '新建修订' }).click()
  577 |   await acceptWorkbenchConfirmation(page)
  578 |   await expect(page.getByTestId('calibration-version-save')).toBeVisible()
  579 |   await page.getByRole('dialog', { name: '编辑手工校准版本' })
  580 |     .getByRole('button', { name: '取消', exact: true }).click()
  581 | })
  582 | 
```