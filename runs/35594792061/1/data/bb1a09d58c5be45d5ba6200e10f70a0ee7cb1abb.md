# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: l1/user-calibration.spec.ts >> @L1 PostgreSQL 用户手工校准UI闭环与训练数值应用
- Location: e2e/specs/l1/user-calibration.spec.ts:64:1

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByRole('heading', { name: '用户精度校准' })
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 5000ms
  - waiting for getByRole('heading', { name: '用户精度校准' })

```

```yaml
- banner:
  - text: ZRT AI负载建模仿真平台
  - link "返回主页":
    - /url: /zrt-sim/
    - img
    - text: 返回主页
  - link "任务管理":
    - /url: /zrt-sim/tasks
  - link "资产管理":
    - /url: /zrt-sim/assets
  - link "信息统计":
    - /url: /zrt-sim/statistics
  - link "版本信息":
    - /url: /zrt-sim/release-notes
  - link "用户管理":
    - /url: /zrt-sim/user-manage
  - link "用户手册":
    - /url: /zrt-sim/user-manual
  - link "API Docs":
    - /url: "#"
  - img
  - text: pw_user_9_1789991684915 Idle
- main:
  - text: ZRT ✦ Scenario Hub
  - heading "AI负载建模仿真平台" [level=1]
  - paragraph: 选择建模场景，进入对应领域的性能评估、硬件对比与系统寻优工具链。
  - article:
    - text: 🏋️ TRAIN 训练
    - paragraph: 大规模分布式训练性能建模，对比不同并行策略与硬件配置下的训练吞吐与 MFU。
    - text: 性能评估
    - button "算子性能评估 →"
    - button "模型性能评估 →"
    - text: 多硬件性能对比
    - button "多硬件性能对比 →"
    - text: 系统寻优
    - button "模型策略自动寻优 →"
    - button "批量训练实验 →"
    - button "硬件寻优 →"
    - button "硬件配比量化分析 →"
  - article:
    - text: ⚡ INFER 推理
    - paragraph: LLM 推理负载建模，分析 Prefill / Decode 阶段的时延、吞吐与显存占用。
    - text: 性能评估
    - button "算子性能评估 →"
    - button "模型性能评估 →"
    - text: 多硬件性能对比
    - button "多硬件性能对比 →"
    - text: 系统寻优
    - button "模型策略自动寻优 →"
    - button "批量推理实验 →"
    - button "PD分离实例配比寻优 →"
  - article:
    - text: 🎯 REC 推荐
    - paragraph: 推荐系统建模，评估 Embedding、特征交互等算子的计算与通信瓶颈。
    - text: 性能评估
    - button "算子性能评估 →"
    - button "模型性能评估 →"
```

# Test source

```ts
  1   | import { test, expect } from '../../fixtures/test'
  2   | import { TaskHistoryPage } from '../../pages/task-history.page'
  3   | import { TrainingModelPage } from '../../pages/training-model.page'
  4   | import { selectOption } from '../../support/select-option'
  5   | import { E2E_MULTI_TASK_TIMEOUT_MS } from '../../support/timeouts'
  6   | 
  7   | type Json = Record<string, any>
  8   | 
  9   | async function submitTrainingFromUi(page, api, trainingPage): Promise<{ body: Json; job: Json }> {
  10  |   const requestPromise = page.waitForRequest(request => (
  11  |     request.method() === 'POST' && request.url().endsWith('/api/train/estimate')
  12  |   ))
  13  |   const responsePromise = page.waitForResponse(response => (
  14  |     response.request().method() === 'POST'
  15  |     && response.url().endsWith('/api/train/estimate')
  16  |     && response.status() === 202
  17  |   ))
  18  |   await trainingPage.runButton.click()
  19  |   const [request, response] = await Promise.all([requestPromise, responsePromise])
  20  |   const submitted = await response.json()
  21  |   return {
  22  |     body: request.postDataJSON(),
  23  |     job: await api.waitForJob(String(submitted.run_id), 'succeeded'),
  24  |   }
  25  | }
  26  | 
  27  | function evidence(result: Json): Json {
  28  |   expect(result.user_calibration?.resolution_count).toBeGreaterThan(0)
  29  |   expect(result.user_calibration?.details?.length).toBeGreaterThan(0)
  30  |   return result.user_calibration
  31  | }
  32  | 
  33  | function taskConfigWithoutCalibration(body: Json): Json {
  34  |   const clone = structuredClone(body)
  35  |   delete clone.calibration_selection
  36  |   return clone
  37  | }
  38  | 
  39  | function inferenceTimingAnchor(result: Json): Json {
  40  |   const inference = result.results?.[0]?.result
  41  |   expect(inference.total_latency_ms).toBeGreaterThan(0)
  42  |   expect(inference.prefill_latency_ms).toBeGreaterThan(0)
  43  |   expect(inference.tpot_ms).toBeGreaterThan(0)
  44  |   return {
  45  |     total_latency_ms: inference.total_latency_ms,
  46  |     prefill_latency_ms: inference.prefill_latency_ms,
  47  |     tpot_ms: inference.tpot_ms,
  48  |     qps: inference.qps,
  49  |     tps: inference.tps,
  50  |   }
  51  | }
  52  | 
  53  | async function waitForNewRun(api, taskId: number, previousRunId: string): Promise<Json> {
  54  |   const deadline = Date.now() + E2E_MULTI_TASK_TIMEOUT_MS
  55  |   while (Date.now() < deadline) {
  56  |     const task = await api.getTask(taskId)
  57  |     const runId = String(task.latest_run_key || task.latest_run_id || '')
  58  |     if (runId && runId !== previousRunId) return api.waitForJob(runId, 'succeeded')
  59  |     await new Promise(resolve => setTimeout(resolve, 1000))
  60  |   }
  61  |   throw new Error(`任务 ${taskId} 未产生新的重跑 attempt`)
  62  | }
  63  | 
  64  | test('@L1 PostgreSQL 用户手工校准UI闭环与训练数值应用', async ({ page, api, catalogs }) => {
  65  |   test.setTimeout(E2E_MULTI_TASK_TIMEOUT_MS)
  66  |   const versionName = `E2E手工校准-${Date.now()}`
  67  |   const trainScope = `${catalogs.train.hardwareName}:${catalogs.train.modelId}:train`
  68  | 
  69  |   await page.goto('/zrt-sim/calibration')
> 70  |   await expect(page.getByRole('heading', { name: '用户精度校准' })).toBeVisible()
      |                                                               ^ Error: expect(locator).toBeVisible() failed
  71  | 
  72  |   await page.getByTestId('calibration-create-open').click()
  73  |   await page.getByTestId('calibration-version-name').fill(versionName)
  74  |   await page.getByTestId('calibration-version-hardware').fill(catalogs.train.hardwareName)
  75  |   await page.getByTestId('calibration-version-model').fill(catalogs.train.modelId)
  76  |   await page.getByTestId('calibration-version-phase').selectOption('train')
  77  |   await page.getByTestId('calibration-entry-operator').fill('aten.mm.default')
  78  |   await page.getByTestId('calibration-entry-unit').selectOption('Cube')
  79  |   await page.getByTestId('calibration-entry-dtype').fill('*')
  80  |   await page.getByTestId('calibration-entry-utilization').fill('25')
  81  |   await page.getByTestId('calibration-entry-add').click()
  82  |   await page.getByTestId('calibration-version-save').click()
  83  |   await page.getByTestId('calibration-nav-versions').click()
  84  | 
  85  |   const versionRow = page.locator('tbody tr').filter({ hasText: versionName })
  86  |   await expect(versionRow).toBeVisible()
  87  |   await versionRow.getByRole('button', { name: '编辑' }).click()
  88  |   await page.getByTestId('calibration-saved-entry-utilization').fill('30')
  89  |   await page.getByTestId('calibration-version-save').click()
  90  |   await versionRow.getByRole('button', { name: '发布' }).click()
  91  |   await expect(versionRow).toContainText('已发布')
  92  |   await versionRow.getByRole('button', { name: '启用' }).click()
  93  |   await expect(versionRow).toContainText('已启用')
  94  | 
  95  |   await page.getByTestId('calibration-nav-rules').click()
  96  |   await page.getByTestId('calibration-rule-hardware').fill(catalogs.train.hardwareName)
  97  |   await page.getByTestId('calibration-rule-model').fill(catalogs.train.modelId)
  98  |   await page.getByTestId('calibration-rule-phase').selectOption('train')
  99  |   await page.getByTestId('calibration-rule-load').click()
  100 |   await page.getByTestId('calibration-rule-version').filter({ hasText: versionName }).click()
  101 |   await page.getByTestId('calibration-rule-save').click()
  102 |   await page.getByTestId('calibration-preview-operator').fill('aten.mm.default')
  103 |   await page.getByTestId('calibration-preview-dtype').fill('bf16')
  104 |   await page.getByTestId('calibration-preview-run').click()
  105 |   await expect(page.getByTestId('calibration-preview-result')).toContainText('30%')
  106 | 
  107 |   const trainingPage = new TrainingModelPage(page)
  108 |   await trainingPage.goto()
  109 |   await trainingPage.fillForm({
  110 |     modelId: catalogs.train.modelId,
  111 |     presetId: catalogs.train.hardwareName,
  112 |     tp: 1, pp: 1, ep: 1, cp: 1,
  113 |   })
  114 | 
  115 |   await page.getByTestId('estimate-calibration-open').click()
  116 |   await expect(page.getByTestId('task-calibration-current-rule')).toBeChecked()
  117 |   await page.getByTestId('task-calibration-done').click()
  118 |   const baseline = await submitTrainingFromUi(page, api, trainingPage)
  119 |   expect(baseline.body.calibration_selection.scope_keys).toContain(trainScope)
  120 |   expect(baseline.body.calibration_selection.overrides).toEqual({})
  121 |   expect(evidence(baseline.job.result).details).toEqual(expect.arrayContaining([
  122 |     expect.objectContaining({
  123 |       scope_key: trainScope, match_kind: 'manual_operator', utilization: 0.3,
  124 |     }),
  125 |   ]))
  126 | 
  127 |   await page.getByTestId('estimate-calibration-open').click()
  128 |   await page.getByTestId('task-calibration-add-override').click()
  129 |   await page.getByTestId('task-calibration-override-operator').fill('aten.mm.default')
  130 |   await page.getByTestId('task-calibration-override-unit').selectOption('Cube')
  131 |   await page.getByTestId('task-calibration-override-dtype').fill('*')
  132 |   await page.getByTestId('task-calibration-override-utilization').fill('50')
  133 |   await page.getByTestId('task-calibration-done').click()
  134 |   const calibrated = await submitTrainingFromUi(page, api, trainingPage)
  135 | 
  136 |   expect(taskConfigWithoutCalibration(calibrated.body)).toEqual(
  137 |     taskConfigWithoutCalibration(baseline.body),
  138 |   )
  139 |   expect(calibrated.body.calibration_selection.overrides.by_scope[trainScope].entries)
  140 |     .toEqual([expect.objectContaining({ utilization: 0.5 })])
  141 |   expect(evidence(calibrated.job.result).details).toEqual(expect.arrayContaining([
  142 |     expect.objectContaining({
  143 |       scope_key: trainScope, match_kind: 'task_override', utilization: 0.5,
  144 |     }),
  145 |   ]))
  146 | 
  147 |   const baselineStepMs = baseline.job.result.data.step_time_ms
  148 |   const calibratedStepMs = calibrated.job.result.data.step_time_ms
  149 |   expect(baselineStepMs).toBeGreaterThan(calibratedStepMs)
  150 |   expect(calibratedStepMs).not.toBe(baselineStepMs)
  151 | 
  152 |   const inferHardware = await api.firstInferenceHardware()
  153 |   const inferModel = await api.firstInferenceModel()
  154 |   const inferScope = `${inferHardware.name}:${inferModel.name}:prefill`
  155 |   const decodeScope = `${inferHardware.name}:${inferModel.name}:decode`
  156 |   const inferVersion = await api.createManualCalibration({
  157 |     name: 'E2E infer prerequisite', hardware_id: inferHardware.name,
  158 |     model_id: inferModel.name, phase: 'prefill', operator: '*',
  159 |     unit: 'Cube', dtype: '*', utilization: 0.25,
  160 |   })
  161 |   await api.publishEnableAndSetDefault(inferScope, inferVersion)
  162 |   const decodeVersion = await api.createManualCalibration({
  163 |     name: 'E2E decode prerequisite', hardware_id: inferHardware.name,
  164 |     model_id: inferModel.name, phase: 'decode', operator: '*',
  165 |     unit: 'Cube', dtype: '*', utilization: 0.25,
  166 |   })
  167 |   await api.publishEnableAndSetDefault(decodeScope, decodeVersion)
  168 | 
  169 |   await page.goto('/zrt-sim/infer/performance/model')
  170 |   const modelInput = page.locator('#model-select-name')
```