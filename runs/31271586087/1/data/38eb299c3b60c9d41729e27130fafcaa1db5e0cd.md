# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: l0/user-manual-navigation.spec.ts >> @L0 管理员可见 API Docs
- Location: e2e/specs/l0/user-manual-navigation.spec.ts:23:1

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByRole('link', { name: 'API Docs' })
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 5000ms
  - waiting for getByRole('link', { name: 'API Docs' })

```

```yaml
- banner:
  - link "首页":
    - /url: /zrt-sim/
    - img
  - text: ZRT AI负载建模仿真平台
  - link "任务管理":
    - /url: /zrt-sim/tasks
  - link "资产管理":
    - /url: /zrt-sim/assets
  - link "信息统计":
    - /url: /zrt-sim/statistics
  - link "版本信息":
    - /url: /zrt-sim/release-notes
  - link "用户管理":
    - /url: /zrt-sim/user-manage
  - link "用户手册":
    - /url: /zrt-sim/user-manual
  - text: Idle
- main:
  - text: ZRT ✦ Scenario Hub
  - heading "AI负载建模仿真平台" [level=1]
  - paragraph: 选择建模场景，进入对应领域的性能评估、硬件对比与系统寻优工具链。
  - article:
    - text: 🏋️ TRAIN 训练
    - paragraph: 大规模分布式训练性能建模，对比不同并行策略与硬件配置下的训练吞吐与 MFU。
    - text: 性能评估
    - button "算子性能评估 →"
    - button "模型性能评估 →"
    - text: 多硬件性能对比
    - button "多硬件性能对比 →"
    - text: 系统寻优
    - button "模型策略自动寻优 →"
    - button "批量训练实验 →"
    - button "硬件寻优 →"
    - button "硬件配比量化分析 →"
  - article:
    - text: ⚡ INFER 推理
    - paragraph: LLM 推理负载建模，分析 Prefill / Decode 阶段的时延、吞吐与显存占用。
    - text: 性能评估
    - button "算子性能评估 →"
    - button "模型性能评估 →"
    - text: 多硬件性能对比
    - button "多硬件性能对比 →"
    - text: 系统寻优
    - button "模型策略自动寻优 →"
    - button "批量推理实验 →"
    - button "PD分离实例配比寻优 →"
  - article:
    - text: 🎯 REC 推荐
    - paragraph: 推荐系统建模，评估 Embedding、特征交互等算子的计算与通信瓶颈。
    - text: 性能评估
    - button "算子性能评估 →"
    - button "模型性能评估 →"
```

# Test source

```ts
  1  | import { test, expect } from '../../fixtures/test'
  2  | 
  3  | test('@L0 普通用户可打开用户手册且不可见 API Docs', async ({ page }) => {
  4  |   await page.goto('/zrt-sim/?account=guide-user&name=Guide%20User')
  5  | 
  6  |   await expect(page.getByRole('link', { name: '用户手册' })).toBeVisible()
  7  |   await expect(page.getByRole('link', { name: 'API Docs' })).toHaveCount(0)
  8  | 
  9  |   await page.goto('/zrt-sim/user-manual?account=guide-user&name=Guide%20User')
  10 |   await expect(page.getByRole('heading', { name: 'AI 负载建模仿真平台用户手册' })).toBeVisible()
  11 |   await expect(page.locator('.manual-content img')).toHaveCount(18)
  12 |   await expect.poll(async () => page.locator('.manual-content img').evaluateAll(images =>
  13 |     images.filter(image => (image as HTMLImageElement).naturalWidth > 0).length,
  14 |   )).toBe(18)
  15 |   await expect(page.locator('.manual-content img').first()).toHaveAttribute('src', /user-manual\/images/)
  16 | 
  17 |   const firstTocLink = page.locator('.manual-content a').filter({ hasText: '进入平台、准备账号和认识常用控件' }).first()
  18 |   await firstTocLink.click()
  19 |   await expect.poll(() => page.evaluate(() => decodeURIComponent(window.location.hash))).toBe('#1-先了解平台')
  20 |   await expect(page.locator('[id="1-先了解平台"]')).toBeVisible()
  21 | })
  22 | 
  23 | test('@L0 管理员可见 API Docs', async ({ page }) => {
  24 |   await page.goto('/zrt-sim/?account=release-admin&name=Release%20Admin')
  25 | 
  26 |   await expect(page.getByRole('link', { name: '用户手册' })).toBeVisible()
> 27 |   await expect(page.getByRole('link', { name: 'API Docs' })).toBeVisible()
     |                                                              ^ Error: expect(locator).toBeVisible() failed
  28 | })
  29 | 
```