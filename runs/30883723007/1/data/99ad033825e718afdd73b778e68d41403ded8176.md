# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: l1/inference-compare.spec.ts >> @L1 推理硬件性能对比
- Location: e2e/specs/l1/inference-compare.spec.ts:3:1

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: locator('.cr-section-card').filter({ hasText: '硬件性能对比' })
Expected: visible
Timeout: 120000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 120000ms
  - waiting for locator('.cr-section-card').filter({ hasText: '硬件性能对比' })

```

```yaml
- banner:
  - button "返回":
    - img
  - link "首页":
    - /url: /zrt-sim/
    - img
  - text: ZRT AI负载建模仿真平台
  - link "任务管理":
    - /url: /zrt-sim/tasks
  - link "资产管理":
    - /url: /zrt-sim/assets
  - link "信息统计":
    - /url: /zrt-sim/statistics
  - link "用户管理":
    - /url: /zrt-sim/user-manage
  - link "API Docs":
    - /url: /zrt-sim-server/docs
  - text: Idle
- main:
  - link "ZRT":
    - /url: /zrt-sim/
  - text: /
  - link "推理":
    - /url: /zrt-sim/
  - text: / 多硬件性能对比
  - button "1 ✓ 选择模型"
  - button "2 ✓ 配置推理负载"
  - button "3 ✓ 选择对比硬件"
  - button "4 性能对比结果"
  - paragraph: 正在对 1 个模型执行自动寻优，已等待 76s...
  - button "取消"
  - text: ✅ 创建任务成功！任务ID：
  - strong: "4"
  - text: ，任务名称：
  - strong: 「deepseek-v4-flash」性能对比
  - text: ， 可在
  - link "「任务管理」":
    - /url: /zrt-sim/tasks
  - text: 中监控运行状态
```

# Test source

```ts
  1  | import { test, expect } from '../../fixtures/test'
  2  | 
  3  | test('@L1 推理硬件性能对比', async ({ page }) => {
  4  |   test.setTimeout(180_000)
  5  | 
  6  |   // 对页面初始化设置明确上限，避免硬件目录异常时挂起整个门禁。
  7  |   await page.goto('/zrt-sim/infer/compare', { waitUntil: 'domcontentloaded', timeout: 15_000 })
  8  |   await expect(page.locator('.model-left-item').first()).toBeVisible({ timeout: 15_000 })
  9  | 
  10 |   // Step 1: 保留一个模型，控制门禁执行规模。
  11 |   const modelCheckboxes = page.locator('.model-left-item input[type="checkbox"]')
  12 |   for (let index = 1; index < await modelCheckboxes.count(); index += 1) {
  13 |     if (await modelCheckboxes.nth(index).isChecked()) await modelCheckboxes.nth(index).click()
  14 |   }
  15 |   await page.getByRole('button', { name: '下一步 →', exact: true }).click()
  16 | 
  17 |   // Step 2: 负载
  18 |   const inputField = page.locator('.ci-field').filter({ hasText: '输入长度' }).locator('input')
  19 |   const outputField = page.locator('.ci-field').filter({ hasText: '输出长度' }).locator('input')
  20 |   await inputField.fill('1024')
  21 |   await outputField.fill('128')
  22 |   await page.getByRole('button', { name: '下一步 →', exact: true }).click()
  23 | 
  24 |   // Step 3: 当前页面默认启用硬件；保留前两个进行对比。
  25 |   const hardwareCheckboxes = page.locator('.hw-left-item input[type="checkbox"]')
  26 |   for (let index = 2; index < await hardwareCheckboxes.count(); index += 1) {
  27 |     if (await hardwareCheckboxes.nth(index).isChecked()) await hardwareCheckboxes.nth(index).click()
  28 |   }
  29 |   await page.getByRole('button', { name: '开始对比 →', exact: true }).click()
  30 | 
  31 |   // Step 4 结果
> 32 |   await expect(page.locator('.cr-section-card').filter({ hasText: '硬件性能对比' })).toBeVisible({ timeout: 120_000 })
     |                                                                                ^ Error: expect(locator).toBeVisible() failed
  33 |   await expect(page.locator('.cr-compare-table')).toBeVisible()
  34 | })
  35 | 
```