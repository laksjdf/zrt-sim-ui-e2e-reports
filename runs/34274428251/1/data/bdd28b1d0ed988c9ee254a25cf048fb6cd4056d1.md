# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: l1/inference-optimize.spec.ts >> @L1 Prefill 重用配置兼容 禁用 Chunked Prefill Size
- Location: e2e/specs/l1/inference-optimize.spec.ts:118:3

# Error details

```
Error: expect(page).toHaveURL(expected) failed

Expected pattern: /step=select/
Received string:  "http://127.0.0.1:52832/zrt-sim/infer/optimize/strategy?task_id=prefill-restore&restore=1&step=workload"
Timeout: 5000ms

Call log:
  - Expect "toHaveURL" with timeout 5000ms
    14 × unexpected value "http://127.0.0.1:52832/zrt-sim/infer/optimize/strategy?task_id=prefill-restore&restore=1&step=workload"

```

# Test source

```ts
  1   | import { test, expect } from '../../fixtures/test'
  2   | import { E2E_TASK_TIMEOUT_MS, E2E_TEST_TIMEOUT_MS } from '../../support/timeouts'
  3   | import { selectOption } from '../../support/select-option'
  4   | 
  5   | test('@L1 推理策略寻优与应用策略', async ({ page }) => {
  6   |   test.setTimeout(E2E_TEST_TIMEOUT_MS)
  7   | 
  8   |   await page.goto('/zrt-sim/infer/optimize/strategy')
  9   |   await expect(page.getByRole('heading', {
  10  |     name: /^(模型策略自动寻优|LLM 负载建模仿真)$/,
  11  |   })).toBeVisible()
  12  | 
  13  |   // Step 1: 选择第一个可用的模型（若页面已默认选中则直接继续）
  14  |   const modelInput = page.locator('#model-select-name')
  15  |   const current = await modelInput.inputValue().catch(() => '')
  16  |   if (!current) {
  17  |     await modelInput.click()
  18  |     const firstOption = page.locator('.model-name-option').first()
  19  |     await firstOption.click()
  20  |   }
  21  |   await page.getByTestId('infer-step-1-continue').click()
  22  | 
  23  |   // Step 2: 自动寻优范围
  24  |   await expect(page.getByRole('button', { name: /(寻优配置|负载配置)/ })).toBeVisible()
  25  |   await page.getByTestId('infer-step-2-continue').click()
  26  | 
  27  |   // Step 3: 选择硬件
  28  |   // 选择 72 卡 B200 POD，避免依赖后端资产目录顺序变化。
  29  |   await page.locator('.hw-chip-select-col').filter({ hasText: '硬件厂商' }).locator('select').selectOption({ label: 'NVIDIA' })
  30  |   await selectOption(page.getByTestId('infer-hardware-select'), 'B200_POD')
  31  |   await page.getByTestId('infer-step-3-run').click()
  32  | 
  33  |   // Step 4 寻优结果
  34  |   await expect(page.locator('.opt-candidates-table').first()).toBeVisible({ timeout: E2E_TASK_TIMEOUT_MS })
  35  | 
  36  |   // 按状态筛选
  37  |   await page.locator('.opt-candidates-wrap').locator('.opt-filter-select').first().selectOption('meets')
  38  |   await expect(page.locator('.opt-candidates-table').first()).toBeVisible()
  39  | 
  40  |   // 应用策略
  41  |   const optimalTable = page.locator('.opt-candidates-wrap').filter({
  42  |     has: page.getByRole('button', { name: '应用策略' }),
  43  |   })
  44  |   await optimalTable.locator('tbody tr').first().click()
  45  |   await optimalTable.getByRole('button', { name: '应用策略' }).click()
  46  |   await expect(page).toHaveURL(/\/infer\/performance\/model/)
  47  | })
  48  | 
  49  | async function mockPrefillTask(page: import('@playwright/test').Page, chunkSize: number | null = 2048) {
  50  |   const model = {
  51  |     name: 'prefill-restore-model', domain: 'infer', operators: [], edges: [], ranks: [],
  52  |     hf_config_json: { hidden_size: 4096, num_hidden_layers: 2, vocab_size: 32000 },
  53  |   }
  54  |   const responses: Record<string, unknown> = {
  55  |     '/api/assets/models': [{ name: model.name, domain: 'infer', builtin: 1 }],
  56  |     '/api/infer/operators': [],
  57  |     '/api/infer/modules': [],
  58  |     [`/api/infer/saved-models/${model.name}`]: model,
  59  |     '/api/tasks/prefill-restore': { operation: 'optimize', config_id: 'prefill-config' },
  60  |     '/api/configs/prefill-config': {
  61  |       config_json: {
  62  |         model_name: model.name, model_json: model,
  63  |         pd_separation: { scenario: 'prefill' },
  64  |         workload: {
  65  |           request: { phase: 'prefill', input_length: 8192, ...(chunkSize === null ? {} : { chunked_prefill_size: chunkSize }) },
  66  |           quant: {
  67  |             quant_global: 'int8', quant_attn: 'bf16', quant_mlp: 'fp4',
  68  |             quant_shared_expert: 'int4', quant_routed_expert: 'int8',
  69  |             quant_kv_cache: 'bf16', quant_activation: 'int8',
  70  |           },
  71  |           optimize: { target_ttft_ms: 1500, min_world_size: 8, max_world_size: 16 },
  72  |         },
  73  |       },
  74  |     },
  75  |   }
  76  |   await page.route('**/zrt-sim-server/api/**', async route => {
  77  |     const pathname = new URL(route.request().url()).pathname
  78  |     const apiPath = pathname.slice(pathname.indexOf('/api/'))
  79  |     await route.fulfill({ json: responses[apiPath] ?? {} })
  80  |   })
  81  |   await page.goto('/zrt-sim/infer/optimize/strategy?task_id=prefill-restore&restore=1&step=workload')
> 82  |   await expect(page).toHaveURL(/step=select/)
      |                      ^ Error: expect(page).toHaveURL(expected) failed
  83  |   await expect(page.getByTestId('infer-step-1-continue')).toBeVisible()
  84  |   await page.getByTestId('infer-step-1-continue').click()
  85  |   await expect(page.getByTestId('infer-step-2-continue')).toBeVisible()
  86  | }
  87  | 
  88  | function chunkSizeInput(page: import('@playwright/test').Page) {
  89  |   return page.locator('.req-card').filter({ hasText: 'Chunked Prefill Size' }).locator('input')
  90  | }
  91  | 
  92  | async function expectRestoredQuant(page: import('@playwright/test').Page) {
  93  |   const labels = ['全局', 'Attention', 'MLP', '共享专家', '路由专家', 'KV Cache', '激活值']
  94  |   const values = ['int8', 'bf16', 'fp4', 'int4', 'int8', 'bf16', 'int8']
  95  |   for (const [index, label] of labels.entries()) {
  96  |     const field = page.locator('.quant-item').filter({ has: page.locator('.quant-label', { hasText: label }) })
  97  |     await expect(field.locator('select')).toHaveValue(values[index])
  98  |   }
  99  | }
  100 | 
  101 | test('@L1 Prefill 重用配置恢复 Chunked Prefill Size', async ({ page }) => {
  102 |   // 原理：拦截历史配置响应并走真实恢复入口；观测点是输入框回填持久化的 2048。
  103 |   await mockPrefillTask(page)
  104 |   await expect(chunkSizeInput(page)).toHaveValue('2048')
  105 | })
  106 | 
  107 | test('@L1 Prefill 重用配置在第 2 → 1 → 2 步保留量化和编辑值', async ({ page }) => {
  108 |   // 原理：卸载并重新挂载配置面板；观测点是七项量化和用户编辑的 chunk size 均保持不变。
  109 |   await mockPrefillTask(page)
  110 |   await expectRestoredQuant(page)
  111 |   await chunkSizeInput(page).fill('4096')
  112 |   await page.getByRole('button', { name: '← 上一步', exact: true }).click()
  113 |   await page.getByTestId('infer-step-1-continue').click()
  114 |   await expectRestoredQuant(page)
  115 |   await expect(chunkSizeInput(page)).toHaveValue('4096')
  116 | })
  117 | for (const chunkSize of [0, null]) {
  118 |   test(`@L1 Prefill 重用配置兼容 ${chunkSize === null ? '缺失' : '禁用'} Chunked Prefill Size`, async ({ page }) => {
  119 |     // 原理：旧配置缺失字段和显式禁用都应回填 0；观测点是输入框的具体值。
  120 |     await mockPrefillTask(page, chunkSize)
  121 |     await expect(chunkSizeInput(page)).toHaveValue('0')
  122 |     await expectRestoredQuant(page)
  123 |   })
  124 | }
  125 | 
```