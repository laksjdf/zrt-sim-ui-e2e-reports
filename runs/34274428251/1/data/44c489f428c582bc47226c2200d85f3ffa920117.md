# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: l1/training-compare.spec.ts >> @L1 训练多硬件性能对比
- Location: e2e/specs/l1/training-compare.spec.ts:4:1

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: getByRole('heading', { name: /多硬件性能对比|Compare/ })
Expected: visible
Timeout: 5000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 5000ms
  - waiting for getByRole('heading', { name: /多硬件性能对比|Compare/ })

```

```
Error: page.waitForResponse: Test ended.
```

# Test source

```ts
  1  | ﻿import { test, expect } from '../../fixtures/test'
  2  | import { E2E_TEST_TIMEOUT_MS } from '../../support/timeouts'
  3  | 
  4  | test('@L1 训练多硬件性能对比', async ({ page, api, catalogs }) => {
  5  |   test.setTimeout(E2E_TEST_TIMEOUT_MS)
  6  | 
> 7  |   const hardwareCatalogResponse = page.waitForResponse(response => {
     |                                        ^ Error: page.waitForResponse: Test ended.
  8  |     if (response.request().method() !== 'GET') return false
  9  |     const url = new URL(response.url())
  10 |     return url.pathname.endsWith('/api/assets/hardwares')
  11 |       && url.searchParams.get('domain') === 'train'
  12 |       && response.status() === 200
  13 |   })
  14 |   await page.goto('/zrt-sim/train/compare')
  15 |   await expect(page.getByRole('heading', { name: /多硬件性能对比|Compare/ })).toBeVisible()
  16 | 
  17 |   const hardwareRows = await (await hardwareCatalogResponse).json() as Array<{
  18 |     name?: string
  19 |     label?: string
  20 |   }>
  21 |   const expectedLabels = hardwareRows
  22 |     .filter(row => row.name)
  23 |     .map(row => row.label || row.name || '')
  24 |   const firstHardwareSelect = page.getByTestId('compare-hardware-select').first()
  25 |   await firstHardwareSelect.click()
  26 |   const renderedLabels = page.locator('.hardware-metric-select-menu:visible .hardware-metric-option > span')
  27 |   await expect(renderedLabels).toHaveCount(expectedLabels.length)
  28 |   await expect(renderedLabels).toHaveText(expectedLabels)
  29 |   await page.keyboard.press('Escape')
  30 | 
  31 |   await page.getByTestId('compare-model-select').selectOption(catalogs.train.modelId)
  32 | 
  33 |   // 确保有两种硬件（add 后默认会选中下一个未使用的硬件）
  34 |   await page.getByTestId('compare-add-hardware').click()
  35 |   const selects = page.getByTestId('compare-hardware-select')
  36 |   await expect(selects).toHaveCount(2)
  37 | 
  38 |   const submitted = page.waitForResponse(response =>
  39 |     response.request().method() === 'POST'
  40 |     && response.url().includes('/api/train/compare')
  41 |     && response.status() === 202,
  42 |   )
  43 |   await page.getByTestId('compare-run-button').click()
  44 |   const job = await (await submitted).json()
  45 | 
  46 |   const backendJob = await api.waitForJob(job.run_id, 'succeeded')
  47 |   await page.getByTestId('compare-result-card').waitFor({ state: 'visible', timeout: 30_000 })
  48 |   await page.getByTestId('compare-result-table').waitFor({ state: 'visible', timeout: 30_000 })
  49 |   expect(backendJob.result?.conclusion?.rows?.length).toBeGreaterThanOrEqual(1)
  50 | 
  51 |   await api.deleteTask(job.task_id)
  52 | })
  53 | 
```